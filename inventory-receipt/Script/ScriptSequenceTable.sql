CREATE TABLE brand_sequences
(
    id BIGINT AUTO_INCREMENT PRIMARY KEY
);

CREATE TABLE category_sequences
(
    id BIGINT AUTO_INCREMENT PRIMARY KEY
);

CREATE TABLE category_product_sequences
(
    id BIGINT AUTO_INCREMENT PRIMARY KEY
);

CREATE TABLE gin_sequences
(
    id BIGINT AUTO_INCREMENT PRIMARY KEY
);

CREATE TABLE gin_product_sequences
(
    id BIGINT AUTO_INCREMENT PRIMARY KEY
);

CREATE TABLE grn_sequences
(
    id BIGINT AUTO_INCREMENT PRIMARY KEY
);

CREATE TABLE grn_product_sequences
(
    id BIGINT AUTO_INCREMENT PRIMARY KEY
);

CREATE TABLE order_sequences
(
    id BIGINT AUTO_INCREMENT PRIMARY KEY
);

CREATE TABLE order_detail_sequences
(
    id BIGINT AUTO_INCREMENT PRIMARY KEY
);

CREATE TABLE product_sequences
(
    id BIGINT AUTO_INCREMENT PRIMARY KEY
);

CREATE TABLE product_price_log_sequences
(
    id BIGINT AUTO_INCREMENT PRIMARY KEY
);

CREATE TABLE refund_information_sequences
(
    id BIGINT AUTO_INCREMENT PRIMARY KEY
);

CREATE TABLE refund_information_detail_sequences
(
    id BIGINT AUTO_INCREMENT PRIMARY KEY
);

CREATE TABLE supplier_sequences
(
    id BIGINT AUTO_INCREMENT PRIMARY KEY
);

CREATE TABLE supplier_group_sequences
(
    id BIGINT AUTO_INCREMENT PRIMARY KEY
);

CREATE TABLE user_sequences
(
    id BIGINT AUTO_INCREMENT PRIMARY KEY
);

CREATE TABLE transaction_sequences
(
    id BIGINT AUTO_INCREMENT PRIMARY KEY
);

CREATE TABLE transaction_category_sequences
(
    id BIGINT AUTO_INCREMENT PRIMARY KEY
);

CREATE TABLE debt_supplier_sequences
(
    id BIGINT AUTO_INCREMENT PRIMARY KEY
);